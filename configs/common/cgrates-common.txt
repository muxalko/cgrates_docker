{
	"general": {
		"rounding_decimals": 5,
		"default_request_type": "*prepaid",
		"default_tenant": "golant",
		"connect_attempts": 10,
		"reconnects": 1,
	},

	"data_db": {
		"db_host": "redis-master.redis",
		"db_name": "0",
		"db_password": "QyV9LVnpbm"
	},

	"stor_db": {
		"db_host": "cgrates-stordb-tp-mysql.mysql",
		"db_user": "cgrates",
		"db_password": "cgrsecret", 
		"max_idle_conns": 0,
		"conn_max_lifetime": 7200,
	},

	"http": {										// HTTP server configuration
		"json_rpc_url": "",					// JSON RPC relative URL ("" to disable)
		"ws_url": "",							// WebSockets relative URL ("" to disable)
		"freeswitch_cdrs_url": "",	// Freeswitch CDRS relative URL ("" to disable)
		"http_cdrs": "",					// CDRS relative URL ("" to disable)
		"use_basic_auth": false,					// use basic authentication
		"auth_users": {},							// basic authentication usernames and base64-encoded passwords (eg: { "username1": "cGFzc3dvcmQ=", "username2": "cGFzc3dvcmQy "})
	},
	
	"listen": {
		"rpc_json": ":2012",
		"rpc_gob": ":2013",				
	},
}